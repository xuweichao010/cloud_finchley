spring:
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        serviceId: config-service
      name: user
      profile: dev
      label: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:10011/eureka