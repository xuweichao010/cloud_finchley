spring:
  application:
    name: auth
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        serviceId: config-service
      name: auth
      profile: dev
      label: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:10011/eureka